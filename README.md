# ALPHA Mobile v0.2
Run: `pip install -r requirements.txt` then `streamlit run app.py`.
For Android after deployment: open its HTTPS address in Chrome and choose Add to Home screen.
Current MVP uses uploaded daily OHLCV CSV data. Automatic NSE data, persistent experiments, walk-forward validation and live execution come later.
